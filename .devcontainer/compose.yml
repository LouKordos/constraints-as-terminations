services:
  cat_sim2real:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
      platforms: !reset []
    volumes:
      # Note that the path of the Dockerfile and context is relative to the *primary* compose.yml
      - .:/app

    command: sleep infinity
